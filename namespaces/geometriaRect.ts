namespace Geometria {
    export namespace Area {
        // para usar as funções guardadas no namespace
        // voce tem que exportar elas usando "export"
        export function retangulo(base: number, alt: number): number {
            return base * alt
        }
    }
}